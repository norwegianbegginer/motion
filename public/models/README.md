# MediaPipe pose model

The app uses Google MediaPipe Pose Landmarker. The bundled model is `pose_landmarker_full.task` and the runtime files are under `public/mediapipe/`.

Enable Mock mode only to test the UI without running the live model.
