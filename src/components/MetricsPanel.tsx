import type { SquatSnapshot } from "../exercises/squat/types";
export function MetricsPanel({s,angle,fps}:{s:SquatSnapshot;angle:number|null;fps:number}){return <section className="metrics"><div><span>Reps</span><strong>{s.reps}</strong></div><div><span>Phase</span><strong>{s.phase}</strong></div><div><span>Knee</span><strong>{angle===null?"—":`${Math.round(angle)}°`}</strong></div><div><span>Current depth</span><strong>{s.currentRepMin===null?"—":`${Math.round(s.currentRepMin)}°`}</strong></div><div><span>Best depth</span><strong>{s.bestDepth===null?"—":`${Math.round(s.bestDepth)}°`}</strong></div><div><span>FPS</span><strong>{Math.round(fps)}</strong></div></section>}
